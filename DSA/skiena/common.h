#ifndef COMMON_H
#define COMMON_H

// Provide: References to integers a and b
// Swaps the integer's addresses to point to each other respectively
void swap_two_integers(int *a, int *b);

// Provide: Integer array and integer n denoting length
// Iterates through elements and prints them
void print_integer_array(int arr[], int n);


#endif